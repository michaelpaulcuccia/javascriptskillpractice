let names = ['michael', 'rentia', 'santino', 'leela', 'capone', 'bruno'];

let lunchMoney = [
    { name: names[0], amount: 25 },
    { name: names[1], amount: 5 },
    { name: names[2], amount: 65 },
    { name: names[3], amount: 45 },
    { name: names[4], amount: 25 }
];

let lastName = ' Cuccia';

/*
names.forEach(output);

function output (item, index, array) {
    console.log(item);
}
*/

//names.forEach(item => console.log(item));

//lunchMoney.forEach((person) => console.log(person));
//lunchMoney.forEach((person) => console.log(person.name.concat(lastName)));

/*
String.prototype.capitalize = function() {
    return this.charAt(0).toUpperCase() + this.slice(1);
}

lunchMoney.forEach((person) => console.log(person.name.capitalize().concat(lastName)));
*/

